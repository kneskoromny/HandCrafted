import SwiftUI

enum Constant {
    
    enum AppFont {
        static let primary: Font = .title3
        static let secondary: Font = .subheadline
        static let thirdly: Font = .footnote
    }
}
